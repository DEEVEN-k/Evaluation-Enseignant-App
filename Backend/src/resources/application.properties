# -----------------------------------------
# Configuration de la base de données MySQL
# -----------------------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/BDDESI?useSSL=false&serverTimezone=UTC
spring.datasource.username=DEEVEN
spring.datasource.password=DEEVENdeeven@9352

# -----------------------------------------
# Configuration JPA / Hibernate
# -----------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# -----------------------------------------
# Configuration du port (optionnel)
# -----------------------------------------
server.port=8081

# -----------------------------------------
# Configuration du logging (optionnel)
# -----------------------------------------
logging.level.org.springframework=INFO
logging.level.com.tonpackage=DEBUG

# -----------------------------------------
# Pour éviter les erreurs de timezone
# -----------------------------------------
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
